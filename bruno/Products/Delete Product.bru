meta {
  name: Delete Product
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/products/:id
  body: none
  auth: none
}

params:path {
  id: {{productId}}
}

headers {
  Authorization: Bearer {{jwt}}
}

vars:pre-request {
  baseUrl: http://localhost:3333
}
